import 'app_database.dart';

class DatabaseSeeder {
  /// Seed initial operational data into SQLite database if empty.
  /// Kept empty to ensure the database starts clean with no default demo projects.
  static Future<void> seedInitialDataIfEmpty(AppDatabase db) async {
    // No default/seeder data
  }
}
